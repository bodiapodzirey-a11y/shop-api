$kind: http-request
name: Create a product
url: "{{baseUrl}}/products"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "name": "test",
      "price": 100,
      "stock": 100,
      "category": "test",
      "description": "test"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "price": {
          "type": "number"
        },
        "stock": {
          "type": "integer"
        },
        "category": {
          "type": "string"
        },
        "description": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "price"
      ]
    }
examples: ./.resources/Create a product.resources/examples
order: 2000
